[project]
name = "flask-jwt-challenge"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Hacker-dev-hub", email = "hackerasdf6789@gmail.com" }
]
requires-python = ">=3.14"
dependencies = []

[project.scripts]
flask-jwt-challenge = "flask_jwt_challenge:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
